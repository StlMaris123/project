<% provide(:title, "Feedback") %>
<h5>Got any isssue that is not clear to you? Talk to your lecturer to have it sorted</h5>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for @feedback  do |f|  %>
            <%= render 'shared/error_messages', object: f.object %>
            <%= f.label :learnt %>
            <%= f.text_area :learnt, class: 'form-control'  %>

            <%= f.label :Problem  %>
            <%= f.text_area :problem, class: 'form-control' %>

            <%= f.label :possible_fix, "How I can be helped" %>
            <%= f.text_area :possible_fix, class: 'form-control' %>

            <%= f.submit "Post Feedback", class: "btn btn-primary" %>

            <% end %>
    
    </div>

</div>
